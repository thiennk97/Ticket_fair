<div class = "container ">
  <div class = "whith-100">
    <h2 class = "text-center mt-5"><%= t ".confirm" %></h2>
    <p class = "mt-3"><%= t ".guide_all_1" %></p>
    <h4 class = "mt-5"><%= t ".code_seat" %></h4>
    <% @tickets.each do |ticket| %>
      <strong class = "bg-warning"><%= ticket.code %></strong>
    <% end %>
    <p class = "mt-5"></p>
    <% @tickets.each do |ticket| %>
      <%= render "tickets/ticket", ticket: ticket %>
    <% end %>
  </div>
</div>
